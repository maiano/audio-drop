# 🚀 Инструкция по деплою

## Локальное тестирование

### 1. Получить Telegram Bot Token

1. Открыть [@BotFather](https://t.me/BotFather) в Telegram
2. Отправить `/newbot`
3. Следовать инструкциям
4. Скопировать токен

### 2. Создать .env файл

```bash
cp .env.example .env
```

Заполнить `.env`:
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
PORT=3000
NODE_ENV=development
LOG_LEVEL=debug
```

### 3. Установить yt-dlp

**macOS:**
```bash
brew install yt-dlp ffmpeg
```

**Linux (Ubuntu/Debian):**
```bash
pip install yt-dlp
apt install ffmpeg
```

**Linux (Alpine - для Docker):**
```bash
apk add python3 py3-pip ffmpeg
pip3 install yt-dlp
```

### 4. Запустить локально

```bash
# Установить зависимости
npm install

# Запустить в dev режиме
npm run dev
```

Бот должен запуститься и вывести:
```
Bot started successfully
Health check server started on port 3000
```

### 5. Протестировать

1. Найти бота в Telegram по username
2. Отправить `/start`
3. Отправить ссылку на YouTube видео
4. Получить аудиофайл

---

## Деплой на Render.com (Бесплатный план)

### Подготовка

1. Зарегистрироваться на [Render.com](https://render.com)
2. Подключить GitHub аккаунт
3. Создать репозиторий и запушить код:

```bash
git add .
git commit -m "feat: initial audio-drop bot implementation"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
```

### Деплой через render.yaml (Рекомендуется)

1. В Render Dashboard нажать "New" → "Blueprint"
2. Выбрать свой GitHub репозиторий
3. Render автоматически обнаружит `render.yaml`
4. Установить environment variable:
   - Кликнуть на созданный сервис
   - Перейти в Environment
   - Добавить: `BOT_TOKEN` = ваш токен от BotFather
5. Нажать "Apply"
6. Дождаться деплоя (5-10 минут)

### Ручной деплой

1. В Render Dashboard нажать "New" → "Web Service"
2. Выбрать GitHub репозиторий
3. Настроить:
   - **Name**: `audio-drop-bot`
   - **Region**: Frankfurt (или ближайший)
   - **Branch**: `main`
   - **Runtime**: Docker
   - **Plan**: Free
4. Advanced Settings:
   - **Health Check Path**: `/health`
   - **Auto-Deploy**: Yes
5. Environment Variables:
   ```
   BOT_TOKEN=<ваш_токен>
   NODE_ENV=production
   LOG_LEVEL=info
   PORT=3000
   ```
6. Нажать "Create Web Service"

### Проверка деплоя

1. Дождаться статуса "Live"
2. Открыть URL сервиса → должен показать `{"service":"audio-drop-bot","status":"running"}`
3. Протестировать бота в Telegram

### Важные замечания

⚠️ **Бесплатный план Render.com**:
- Бот засыпает после 15 минут неактивности
- Первый запрос после пробуждения занимает 30-60 секунд
- 750 часов в месяц (достаточно для одного сервиса)
- 512 МБ RAM (достаточно для этого бота)

💡 **Рекомендации**:
- Для production используйте платный план ($7/месяц)
- Или настройте keep-alive пинги (UptimeRobot, Cronitor)
- Или используйте другие платформы: Fly.io, Railway, Heroku

---

## Деплой через Docker локально

```bash
# Собрать образ
docker build -t audio-drop-bot .

# Запустить
docker run -d \
  --name audio-drop-bot \
  -e BOT_TOKEN=your_token \
  -e PORT=3000 \
  -p 3000:3000 \
  audio-drop-bot

# Посмотреть логи
docker logs -f audio-drop-bot

# Остановить
docker stop audio-drop-bot
docker rm audio-drop-bot
```

### Docker Compose

```bash
# Создать .env файл
cp .env.example .env
# Заполнить BOT_TOKEN в .env

# Запустить
docker-compose up -d

# Логи
docker-compose logs -f

# Остановить
docker-compose down
```

---

## Мониторинг и логи

### Render.com

1. Открыть Dashboard → ваш сервис
2. Вкладка "Logs" - живые логи
3. Вкладка "Metrics" - использование CPU/RAM

### Docker

```bash
# Живые логи
docker logs -f audio-drop-bot

# Последние 100 строк
docker logs --tail 100 audio-drop-bot

# Статистика
docker stats audio-drop-bot
```

---

## Обновление бота

### Render.com

При push в `main` ветку Render автоматически задеплоит:

```bash
git add .
git commit -m "feat: new feature"
git push origin main
```

### Docker

```bash
# Пересобрать образ
docker-compose down
docker-compose build
docker-compose up -d
```

---

## Troubleshooting

### Бот не отвечает

1. Проверить логи: есть ли ошибки?
2. Проверить health endpoint: `curl https://your-app.onrender.com/health`
3. Проверить BOT_TOKEN: правильный ли токен?
4. Проверить yt-dlp: установлен ли в контейнере?

### Ошибка "Video unavailable"

- Видео может быть приватным или с возрастными ограничениями
- Попробовать другое публичное видео

### Ошибка "Failed to start yt-dlp"

- yt-dlp не установлен или не в PATH
- В Docker: проверить Dockerfile
- Локально: установить через `pip install yt-dlp`

### Таймаут при загрузке длинного видео

- Telegram timeout: 120 секунд
- Для длинных видео (>1 час) может не успеть
- Решение: использовать более быструю сеть или сократить длительность

---

## Поддержка

Для вопросов и проблем создавайте issue в GitHub репозитории.
