services:
  - type: web
    name: audio-drop-bot
    runtime: docker
    plan: free
    region: frankfurt
    envVars:
      - key: NODE_ENV
        value: production
      - key: LOG_LEVEL
        value: debug
      - key: PORT
        value: 3000
      - key: WARP_PROXY_URL
        value: socks5://localhost:1080
      - key: BOT_TOKEN
        sync: false
    healthCheckPath: /health
    autoDeploy: true
    branch: main
